# The config recipe.
recipe: default.v1

# The assistant project unique identifier
# This default value must be replaced with a unique assistant name within your deployment
assistant_id: 20250611-095840-charcoal-bongo

language: en
pipeline:
  - name: CompactLLMCommandGenerator
    llm:
      model_group: openai-gpt-4o
    flow_retrieval:
      active: false
    prompt_template: prompt_templates/command_generator_prompt.jinja2

# Configuration for Rasa Core.
policies:
- name: FlowPolicy
